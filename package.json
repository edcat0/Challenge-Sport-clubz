{
  "name": "challenge-sports-clubz",
  "version": "1.3.0",
  "description": "Sport community app for all sports for everyone",
  "scripts": {
    "start": "npx serve .",
    "build": "echo 'Static web assets ready'",
    "cap:sync": "npx cap sync",
    "cap:android": "npx cap add android && npx cap open android",
    "cap:ios": "npx cap add ios && npx cap open ios"
  },
  "author": "edcat0",
  "license": "MIT",
  "devDependencies": {
    "@capacitor/cli": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/android": "^6.0.0",
    "@capacitor/ios": "^6.0.0"
  }
}